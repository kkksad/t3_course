import { StaticImageData } from "next/image";

export interface Countries {
    name:string,
    flag:StaticImageData,
    capital:string,
    Language:string,
    Population:string,
    Currency:string
  }
  

  
